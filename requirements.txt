# ---- Core web app ----
Flask==3.1.1
gunicorn==23.0.0

# ---- Contact form / email (optional but installed) ----
Flask-Mail==0.10.0

# ---- OpenAI + API helpers ----
openai==1.109.1
requests==2.32.4
python-dotenv==1.1.0

# ---- Blog rendering (Markdown posts) ----
Markdown==3.10
markdownify==1.2.2

# ---- Optional: Postgres (keep for future DB support) ----
psycopg2-binary==2.9.11

# ---- Testing only (safe to leave, but optional) ----
pytest==9.0.1
